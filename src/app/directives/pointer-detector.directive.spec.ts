import { PointerDetectorDirective } from './pointer-detector.directive';

describe('PointerDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
