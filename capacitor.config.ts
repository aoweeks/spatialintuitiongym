import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'The Spatial Intuition Gym',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
